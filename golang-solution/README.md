# Golang solution

## Requirements

- Go 1.22+

## Run tests

From this folder:

```bash
go test ./...
```

## Implementation

The function is implemented in `sort.go` as:

- `Sort(width, height, length, mass int) string`

