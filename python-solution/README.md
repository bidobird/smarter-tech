# Python solution

## Requirements

- Python 3.10+

## Run tests

From this folder:

```bash
python -m unittest discover -s tests -p "test_*.py"
```

## Implementation

The function is implemented in `src/sort.py` as:

- `sort(width, height, length, mass) -> "STANDARD" | "SPECIAL" | "REJECTED"`

